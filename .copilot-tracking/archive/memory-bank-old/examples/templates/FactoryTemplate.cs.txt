using System;
using Microsoft.Extensions.Logging;

namespace {NAMESPACE};

/// <summary>
/// Plantilla base para factoría multiclase con clave.
/// </summary>
public class {FACTORY_NAME} : S7Tools.Core.Factories.BaseKeyedFactory<{KEY_TYPE}, {BASE_TYPE}>
{
    public {FACTORY_NAME}(ILogger logger) : base(logger) { }
    protected override void RegisterFactories()
    {
        // Ejemplo:
        // RegisterFactory({KEY_EXAMPLE}, () => new {CONCRETE_TYPE}());
    }
}
